# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
#

# Basic Airflow cluster configuration for CeleryExecutor with Redis and PostgreSQL.
#
# WARNING: This configuration is for local development. Do not use it in a production deployment.
#
# This configuration supports basic configuration using environment variables or an .env file
# The following variables are supported:
#
# AIRFLOW_IMAGE_NAME           - Docker image name used to run Airflow.
#                                Default: apache/airflow:3.2.1
# AIRFLOW_UID                  - User ID in Airflow containers
#                                Default: 50000
# AIRFLOW_PROJ_DIR             - Base path to which all the files will be volumed.
#                                Default: .
# Those configurations are useful mostly in case of standalone testing/running Airflow in test/try-out mode
#
# _AIRFLOW_WWW_USER_USERNAME   - Username for the administrator account (if requested).
#                                Default: airflow
# _AIRFLOW_WWW_USER_PASSWORD   - Password for the administrator account (if requested).
#                                Default: airflow
# _PIP_ADDITIONAL_REQUIREMENTS - Additional PIP requirements to add when starting all containers.
#                                Use this option ONLY for quick checks. Installing requirements at container
#                                startup is done EVERY TIME the service is started.
#                                A better way is to build a custom image or extend the official image
#                                as described in https://airflow.apache.org/docs/docker-stack/build.html.
#                                Default: ''
#
# Feel free to modify this file to suit your needs.
# ---
x-airflow-common:
  &airflow-common
  # In order to add custom dependencies or upgrade provider distributions you can use your extended image.
  # If `build` is present Docker Compose will build the image (tagged as `image`) and you can
  # pass extra pip requirements via the `_PIP_ADDITIONAL_REQUIREMENTS` env var which will be
  # forwarded to the build as a build-arg `PIP_ADDITIONAL_REQUIREMENTS`.
  image: ${AIRFLOW_IMAGE_NAME:-apache/airflow:3.2.1-python3.11}
  build:
    context: ./airflow
    dockerfile: Dockerfile.airflow
    args:
      PIP_ADDITIONAL_REQUIREMENTS: ${_PIP_ADDITIONAL_REQUIREMENTS:-}
  env_file:
    - ${ENV_FILE_PATH:-.env}
  environment:
    &airflow-common-env
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    AIRFLOW__CORE__AUTH_MANAGER: airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres/airflow
    AIRFLOW__CORE__FERNET_KEY: ${FERNET_KEY}
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: 'true'
    AIRFLOW__CORE__LOAD_EXAMPLES: 'true'
    AIRFLOW__CORE__EXECUTION_API_SERVER_URL: 'http://airflow-apiserver:8080/execution/'
    AIRFLOW__API_AUTH__JWT_SECRET: ${AIRFLOW__API_AUTH__JWT_SECRET:-airflow_jwt_secret}
    AIRFLOW__API_AUTH__JWT_ISSUER: ${AIRFLOW__API_AUTH__JWT_ISSUER:-airflow}
    AIRFLOW_CONN_SPARK_DEFAULT: 'spark://spark-master:7077'
    # yamllint disable rule:line-length
    # Use simple http server on scheduler for health checks
    # See https://airflow.apache.org/docs/apache-airflow/stable/administration-and-deployment/logging-monitoring/check-health.html#scheduler-health-check-server
    # yamllint enable rule:line-length
    AIRFLOW__SCHEDULER__ENABLE_HEALTH_CHECK: 'true'
    # WARNING: Use _PIP_ADDITIONAL_REQUIREMENTS option ONLY for a quick checks
    # for other purpose (development, test and especially production usage) build/extend Airflow image.
    _PIP_ADDITIONAL_REQUIREMENTS: ${_PIP_ADDITIONAL_REQUIREMENTS:-}
    # The following line can be used to set a custom config file, stored in the local config folder
    AIRFLOW_CONFIG: '/opt/airflow/config/airflow.cfg'
  volumes:
    - ${AIRFLOW_PROJ_DIR:-./airflow}/dags:/opt/airflow/dags
    - ${AIRFLOW_PROJ_DIR:-./airflow}/logs:/opt/airflow/logs
    - ${AIRFLOW_PROJ_DIR:-./airflow}/config:/opt/airflow/config
    - ${AIRFLOW_PROJ_DIR:-./airflow}/plugins:/opt/airflow/plugins
    - ./spark/app:/opt/spark/app
  user: "${AIRFLOW_UID:-50000}:0"
  networks:
    - data-network
  depends_on:
    &airflow-common-depends-on
    postgres:
      condition: service_healthy

services:

  airflow-apiserver:
    <<: *airflow-common
    command: api-server
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/api/v2/monitor/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      <<: *airflow-common-depends-on
      airflow-init:
        condition: service_completed_successfully

  airflow-scheduler:
    <<: *airflow-common
    command: scheduler
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8974/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      <<: *airflow-common-depends-on
      airflow-init:
        condition: service_completed_successfully

  airflow-dag-processor:
    <<: *airflow-common
    command: dag-processor
    healthcheck:
      test: ["CMD-SHELL", 'airflow jobs check --job-type DagProcessorJob --hostname "$${HOSTNAME}"']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      <<: *airflow-common-depends-on
      airflow-init:
        condition: service_completed_successfully

  airflow-triggerer:
    <<: *airflow-common
    command: triggerer
    healthcheck:
      test: ["CMD-SHELL", 'airflow jobs check --job-type TriggererJob --hostname "$${HOSTNAME}"']
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: always
    depends_on:
      <<: *airflow-common-depends-on
      airflow-init:
        condition: service_completed_successfully

  airflow-init:
    <<: *airflow-common
    entrypoint: /bin/bash
    # yamllint disable rule:line-length
    command:
      - -c
      - |
        if [[ -z "${AIRFLOW_UID}" ]]; then
          echo
          echo -e "\033[1;33mWARNING!!!: AIRFLOW_UID not set!\e[0m"
          echo "If you are on Linux, you SHOULD follow the instructions below to set "
          echo "AIRFLOW_UID environment variable, otherwise files will be owned by root."
          echo "For other operating systems you can get rid of the warning with manually created .env file:"
          echo "    See: https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html#setting-the-right-airflow-user"
          echo
          export AIRFLOW_UID=$$(id -u)
        fi
        one_meg=1048576
        mem_available=$$(($$(getconf _PHYS_PAGES) * $$(getconf PAGE_SIZE) / one_meg))
        cpus_available=$$(grep -cE 'cpu[0-9]+' /proc/stat)
        disk_available=$$(df / | tail -1 | awk '{print $$4}')
        warning_resources="false"
        if (( mem_available < 4000 )) ; then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough memory available for Docker.\e[0m"
          echo "At least 4GB of memory required. You have $$(numfmt --to iec $$((mem_available * one_meg)))"
          echo
          warning_resources="true"
        fi
        if (( cpus_available < 2 )); then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough CPUS available for Docker.\e[0m"
          echo "At least 2 CPUs recommended. You have $${cpus_available}"
          echo
          warning_resources="true"
        fi
        if (( disk_available < one_meg * 10 )); then
          echo
          echo -e "\033[1;33mWARNING!!!: Not enough Disk space available for Docker.\e[0m"
          echo "At least 10 GBs recommended. You have $$(numfmt --to iec $$((disk_available * 1024 )))"
          echo
          warning_resources="true"
        fi
        if [[ $${warning_resources} == "true" ]]; then
          echo
          echo -e "\033[1;33mWARNING!!!: You have not enough resources to run Airflow (see above)!\e[0m"
          echo "Please follow the instructions to increase amount of resources available:"
          echo "   https://airflow.apache.org/docs/apache-airflow/stable/howto/docker-compose/index.html#before-you-begin"
          echo
        fi
        echo
        echo "Creating missing opt dirs if missing:"
        echo
        mkdir -v -p /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Airflow version:"
        /entrypoint airflow version
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Running airflow config list to create default config file if missing."
        echo
        /entrypoint airflow config list >/dev/null
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Change ownership of files in /opt/airflow to ${AIRFLOW_UID}:0"
        echo
        chown -R "${AIRFLOW_UID}:0" /opt/airflow/
        echo
        echo "Change ownership of files in shared volumes to ${AIRFLOW_UID}:0"
        echo
        chown -v -R "${AIRFLOW_UID}:0" /opt/airflow/{logs,dags,plugins,config}
        echo
        echo "Files in shared volumes:"
        echo
        ls -la /opt/airflow/{logs,dags,plugins,config}

    # yamllint enable rule:line-length
    environment:
      <<: *airflow-common-env
      _AIRFLOW_DB_MIGRATE: 'true'
      _AIRFLOW_WWW_USER_CREATE: 'true'
      _AIRFLOW_WWW_USER_USERNAME: ${_AIRFLOW_WWW_USER_USERNAME:-airflow}
      _AIRFLOW_WWW_USER_PASSWORD: ${_AIRFLOW_WWW_USER_PASSWORD:-airflow}
      _PIP_ADDITIONAL_REQUIREMENTS: ''
    user: "0:0"

  airflow-cli:
    <<: *airflow-common
    profiles:
      - debug
    environment:
      <<: *airflow-common-env
      CONNECTION_CHECK_MAX_COUNT: "0"
    # Workaround for entrypoint issue. See: https://github.com/apache/airflow/issues/16252
    command:
      - bash
      - -c
      - airflow
    depends_on:
      <<: *airflow-common-depends-on

# ---------- PostgreSQL ----------
  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    command: ["postgres", "-c", "wal_level=logical"]
    ports:
      - "5432:5432"
    volumes:
      - postgres-db-volume:/var/lib/postgresql/data
    networks:
      - data-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    restart: unless-stopped

  postgres-init:
    image: postgres:16
    container_name: postgres-init
    environment:
      - PGPASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - ./dataset/ecommerce:/data/ecommerce:ro
      - ./Script:/data/sql:ro
    command: >
      bash -c "
      cp -a /data/ecommerce/. /tmp/;
      cp -a /data/sql/. /tmp/;
      psql -h postgres -U ${POSTGRES_USER} -d postgres -tAc \"SELECT 1 FROM pg_database WHERE datname='airflow'\" | grep -q 1 ||
      psql -h postgres -U ${POSTGRES_USER} -d postgres -c \"CREATE DATABASE airflow;\";
      psql -h postgres -U ${POSTGRES_USER} -d postgres -tAc \"SELECT 1 FROM pg_database WHERE datname='metabase'\" | grep -q 1 ||
      psql -h postgres -U ${POSTGRES_USER} -d postgres -c \"CREATE DATABASE metabase;\";
      psql -h postgres -U ${POSTGRES_USER} -d postgres -tAc \"SELECT 1 FROM pg_database WHERE datname='orders'\" | grep -q 1 ||
      psql -h postgres -U ${POSTGRES_USER} -d postgres -c \"CREATE DATABASE orders;\";
      psql -h postgres -U ${POSTGRES_USER} -d orders -f /tmp/create_tables.sql;
      psql -h postgres -U ${POSTGRES_USER} -d orders -f /tmp/import_raw.sql
      "
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - data-network
    restart: "no"

# # ---------- Kafka ----------
#   kafka:
#     image: public.ecr.aws/bitnami/kafka:3.7.0
#     container_name: kafka
#     hostname: kafka
#     environment:
#       - KAFKA_ENABLE_KRAFT=yes
#       - KAFKA_CFG_PROCESS_ROLES=broker,controller
#       - KAFKA_CFG_NODE_ID=1
#       - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=1@kafka:9093
#       - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093
#       - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092      
#       - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
#       - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
#       - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
#       - ALLOW_PLAINTEXT_LISTENER=yes
#     ports:
#       - "9092:9092"
#     healthcheck:
#       test: ["CMD", "kafka-topics.sh", "--bootstrap-server=kafka:9092", "--list"]
#       interval: 10s
#       timeout: 10s
#       retries: 10
#     networks:
#       - data-network
#     restart: unless-stopped

# # ---------- Kafka UI ----------
#   kafka-ui:
#     image: provectuslabs/kafka-ui:latest
#     container_name: kafka-ui
#     ports:
#       - "8084:8080"
#     environment:
#       - KAFKA_CLUSTERS_0_NAME=kraft-cluster
#       - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092
#       - KAFKA_CLUSTERS_0_PROPERTIES_SECURITY_PROTOCOL=PLAINTEXT
#       - KAFKA_CLUSTERS_0_READONLY=false
#       - KAFKA_CLUSTERS_0_KAFKACONNECT_0_NAME=debezium-connect
#       - KAFKA_CLUSTERS_0_KAFKACONNECT_0_ADDRESS=http://connect:8083
#     depends_on:
#       - kafka
#       - connect
#     networks:
#       - data-network
#     restart: unless-stopped

# # -----------Connect----------
#   connect:
#     image: debezium/connect:2.7.0.Final
#     container_name: connect
#     ports:
#       - "8083:8083"
#     environment:
#       - BOOTSTRAP_SERVERS=kafka:9092
#       - GROUP_ID=1
#       - CONFIG_STORAGE_TOPIC=connect_configs
#       - OFFSET_STORAGE_TOPIC=connect_offsets
#       - STATUS_STORAGE_TOPIC=connect_status
#       - KEY_CONVERTER=org.apache.kafka.connect.json.JsonConverter
#       - VALUE_CONVERTER=org.apache.kafka.connect.json.JsonConverter
#       - VALUE_CONVERTER_SCHEMAS_ENABLE=false
#       - KEY_CONVERTER_SCHEMAS_ENABLE=false
#       - ENABLE_DEBEZIUM_SCRIPTING=false
#     depends_on:
#       - kafka
#       - postgres
#     volumes:
#       - ./connectors:/kafka/connectors
#       # - ./connect_data:/kafka/data
#     networks:
#       - data-network
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8083/connectors"]
#       interval: 15s
#       timeout: 10s
#       retries: 10
#       start_period: 30s
#     restart: unless-stopped

# ---------- MinIO ----------
  minio:
    image: minio/minio:latest
    container_name: minio
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./minio_data:/data
    networks:
      - data-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

# ---------- MinIO Init (Create Buckets) ----------
  minio-init:
    image: minio/mc:latest
    container_name: minio-init
    volumes:
      - ./:/data
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD};
      mc mb local/bronze --ignore-existing;
      mc mb local/silver --ignore-existing;
      mc mb local/gold --ignore-existing;
      echo 'Buckets created: bronze, silver, gold';
      exit 0
      "
    networks:
      - data-network
    depends_on:
      minio:
        condition: service_healthy

# ---------- Hive Metastore ----------
  hive-metastore:
    build:
      context: ./hive
      dockerfile: Dockerfile
    image: hive-metastore-s3:4.0.0
    container_name: hive-metastore
    environment:
      - SERVICE_NAME=metastore
      - DB_DRIVER=postgres
      - HIVE_DB_NAME=${POSTGRES_DB}
      - HIVE_DB_HOST=postgres
      - HIVE_DB_PORT=5432
      - HIVE_DB_USER=${POSTGRES_USER}
      - HIVE_DB_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - ./hive/hive-site.xml:/opt/hive/conf/hive-site.xml
    ports:
      - "9083:9083"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - data-network
    healthcheck:
      test: ["CMD", "bash", "-c", "cat < /dev/null > /dev/tcp/localhost/9083"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s
    restart: unless-stopped
  
# ---------- Spark ----------
  spark-master:
    build:
      context: ./spark
      dockerfile: Dockerfile
    image: custom-spark:3.5.1
    container_name: spark-master
    command: bash -c "/opt/spark/bin/spark-class org.apache.spark.deploy.master.Master --host spark-master"
    env_file:
      - .env
    environment:
      - SPARK_MODE=master
      - HOME=/opt/spark/work-dir
      - SPARK_SUBMIT_OPTS=-Divy.home=/opt/spark/work-dir/.ivy2 -Divy.cache.dir=/opt/spark/work-dir/.ivy2/cache
      - KAFKA_BROKER=${KAFKA_BOOTSTRAP_SERVERS}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=us-east-1
      - S3_ENDPOINT=${S3_ENDPOINT}
      - HIVE_METASTORE_URI=${HIVE_METASTORE_URI}
      - PYTHONPATH=/opt/spark/app
    ports:
      - "8085:8080"
      - "7077:7077"
    volumes:
      - ./spark/app:/opt/spark/app
    depends_on:
      - minio
      - hive-metastore
    networks:
      - data-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 10s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2'
    restart: unless-stopped

  spark-worker:
    build:
      context: ./spark
      dockerfile: Dockerfile
    image: custom-spark:3.5.1
    container_name: spark-worker
    command: bash -c "/opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077"
    env_file:
      - .env
    environment:
      - SPARK_MODE=worker
      - HOME=/opt/spark/work-dir
      - SPARK_SUBMIT_OPTS=-Divy.home=/opt/spark/work-dir/.ivy2 -Divy.cache.dir=/opt/spark/work-dir/.ivy2/cache
      - SPARK_MASTER_URL=spark://spark-master:7077
      - KAFKA_BROKER=${KAFKA_BOOTSTRAP_SERVERS}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_REGION=us-east-1
      - S3_ENDPOINT=${S3_ENDPOINT}
      - HIVE_METASTORE_URI=${HIVE_METASTORE_URI}
      - PYTHONPATH=/opt/spark/app
      - SPARK_WORKER_CORES=2
      - SPARK_WORKER_MEMORY=2g
    depends_on:
      - spark-master
    networks:
      - data-network
    volumes:
      - ./spark/app:/opt/spark/app
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: '2'
    restart: unless-stopped

# # ---------- Trino ----------
#   trino:
#     image: trinodb/trino:latest
#     container_name: trino
#     ports:
#       - "8082:8080"
#     volumes:
#       - ./trino/catalog:/etc/trino/catalog
#       - ./trino/config.properties:/etc/trino/config.properties
#     depends_on:
#       hive-metastore:
#         condition: service_healthy
#     networks:
#       - data-network
#     restart: unless-stopped
    
# # ---------- Metabase ----------
#   metabase:
#     image: metabase/metabase:latest
#     container_name: metabase
#     environment:
#       - MB_DB_TYPE=postgres
#       - MB_DB_DBNAME=metabase
#       - MB_DB_PORT=5432
#       - MB_DB_USER=${POSTGRES_USER}
#       - MB_DB_PASS=${POSTGRES_PASSWORD}
#       - MB_DB_HOST=postgres
#     ports:
#       - "3000:3000"
#     volumes:
#       - ./metabase:/metabase-data
#     depends_on:
#       trino:
#         condition: service_started
#       postgres-init:
#         condition: service_completed_successfully
#     networks:
#       - data-network
#     restart: unless-stopped

# # ---------- FastAPI ----------
#   fastapi:
#     build:
#       context: ./api
#       dockerfile: Dockerfile
#     container_name: fastapi
#     env_file:
#       - .env
#     ports:
#       - "8000:8000"
#     volumes:
#       - ./api:/app
#     environment:
#       - TRINO_HOST=trino
#       - TRINO_PORT=8080
#       - TRINO_CATALOG=delta
#       - TRINO_SCHEMA=gold
#       - TRINO_USER=admin
#     depends_on:
#       - trino
#     networks:
#       - data-network
#     restart: unless-stopped

networks:
  data-network:
    driver: bridge

volumes:
  postgres-db-volume: